{
  "name": "webrtc-sfu-pilot",
  "private": true,
  "description": "mediasoup SFU pilot: Layer A+B + optional C1 PlainTransport ingest (FFmpeg)",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.cjs",
    "build:client": "esbuild client/entry.mjs --bundle --format=esm --outfile=public/mediasoup-client.esm.js --platform=browser",
    "c1:ingest": "bash scripts/run-c1-ffmpeg-ingest.sh"
  },
  "dependencies": {
    "express": "^4.21.0",
    "mediasoup": "^3.19.22",
    "mediasoup-client": "^3.7.17",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
